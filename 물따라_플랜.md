# 퐁당 (PongDang) — 종합 구현 계획서

> **물(Water) 특화 여행 플랫폼** 전체 6 Phase 구현 로드맵
> 기획서 [물따라_기획서.md](물따라_기획서.md) 전문 반영

> [!CAUTION]
> 이 문서는 초기 로드맵을 보존한 **비규범·역사 문서**입니다. 아래의 코드와
> 배포 예시는 그대로 실행하지 마세요. 현재 보안·도메인 계약은
> [`AGENTS.md`](AGENTS.md), 실제 개발/운영 명령은 [`README.md`](README.md),
> Raspberry Pi 배포·백업·복구는
> [`docs/operations-runbook.md`](docs/operations-runbook.md)가 기준입니다.

---

## 기획서 ↔ 구현 전체 매핑

### 핵심 3축 (서비스 정체성)
| 축 | 기능 ID | 설명 | 구현 Phase |
|----|---------|------|-----------|
| **지금** | A1 Water Index | 0~100점 실시간 종합 지표, 활동별 가중치 차등 | P3(로직) → P4(API) → P5(UI) |
| **언제** | A2 Water Forecast | 7일치 예보 그래프, 주식 차트형 캘린더 히트맵 | P3(로직) → P4(API) → P5(UI) |
| **직접 눈으로** | A3~A5 Water Twin + 라이브캠 | 디지털 트윈 지도 + 수온 오버레이 + CCTV 모음 | P5(UI) |

### 기능 그룹 전체 매핑표

#### 그룹 A. 컨디션 데이터 (서비스의 심장)
| ID | 기능명 | ERD 모델 | 필요 API | Phase |
|----|--------|----------|----------|-------|
| A1 | **Water Index** [플래그십] | `WaterCondition`, `ConditionScore` | 기상청 + 해양조사원 + 환경부 + 바다누리 | P3→P4→P5 |
| A2 | **Water Forecast** [제2 플래그십] | `WaterForecast` | 기상청 중기예보 + 조석 예보 | P3→P4→P5 |
| A3 | Water Twin (디지털 트윈 지도) | `WaterSpot` + `WaterCondition` | 전체 융합 | P5 |
| A4 | 실시간 수온 지도 (Twin 핵심 레이어) | `WaterCondition.water_temp` | 해양조사원 수온 | P5 |
| A5 | **라이브캠 물멍 허브** [데모 비밀무기] | `WaterSpot.livecam_url` | 지자체 CCTV | P5 |
| A6 | 물때 액티비티 타이머 | `WaterCondition.tide_schedule` | 해양조사원 조석 | P3→P5 |
| A7 | "올해 첫 입수 가능" 알림 | `WaterCondition.water_temp` | 해양조사원 수온 | P3(로직)→P5 |
| A8 | 수질 신뢰도 교차검증 | `WaterCondition.water_quality_grade` + `UserActivity` 리뷰 | 환경부 + 사용자 데이터 | P4→P5 |

#### 그룹 B. AI 추천·컨시어지
| ID | 기능명 | ERD 모델 | 구현 방식 | Phase |
|----|--------|----------|----------|-------|
| B1 | AI 여행 추천 (페르소나 큐레이션) | `User.persona_type`, `UserActivity` | 스와이프 UI + 태그 기반 필터링 | P4→P5 |
| B2 | AI 일정 자동 생성 | `Itinerary` | 출발지/이동수단/예산 → 동선 생성 | P4→P5 |
| B3 | AI Water Concierge (자연어 추천) | `ConditionScore` 기반 | 자연어 입력 → 스팟 랭킹 | P4→P5 |
| B4 | AI Water Companion (실시간 동행) | `WaterCondition` 실시간 | 실시간 조언 (파고/간조/수위 알림) | P5 |
| B5 | Mood Based Water Travel (감정 기반) | `User.mood_state` | 감정 → 힐링형/해소형 분류 | P4→P5 |

#### 그룹 C. 경험·콘텐츠
| ID | 기능명 | ERD 모델 | Phase |
|----|--------|----------|-------|
| C1 | 물멍 지도 | `WaterSpot` (정적↔동적 무드 축) | P5 |
| C2 | Water Sound Library / ASMR 지수 | `SoundProfile` | P3(지수 계산)→P5 |
| C3 | 만조×일몰 골든 캘린더 | `GoldenMoment` | P3(계산)→P5 |
| C4 | Memory Replay (추억 재현) | `TripMemory` | P5 (후순위) |
| C5 | Water Life Analytics (장기 변화) | `SpotAnalytics` | P3(집계)→P5 |

#### 그룹 D. 동선·편의
| ID | 기능명 | ERD 모델 | Phase |
|----|--------|----------|-------|
| D1 | 물놀이 Pre/Post 동선 관리 | `NearbyFacility` (shower/changing_room) | P2(모델)→P4→P5 |
| D2 | 실시간 혼잡도 / 물 반 사람 반 지수 | `CrowdLevel` | P3→P4→P5 |
| D3 | Water & Taste 페어링 (로컬 미식) | `NearbyFacility.tag` (local_food) | P4→P5 |

#### 그룹 E. 게이미피케이션·리텐션
| ID | 기능명 | ERD 모델 | Phase |
|----|--------|----------|-------|
| E1 | Water Passport (방문 인증·배지) | `Passport` | P2(모델)→P4→P5 |
| E2 | Water Collection (도감 시스템) | `Passport.badge_earned` | P4→P5 |
| E3 | 플로깅 & 에코 배지 (ESG) | `Passport.eco_action` | P4→P5 |

#### 그룹 F. 안전·사회가치
| ID | 기능명 | ERD 모델 | Phase |
|----|--------|----------|-------|
| F1 | 계곡 안전 레이더 | `WaterCondition` (수위/유량 + 호우예보) | P3(로직)→P4→P5 |
| F2 | 오프라인 안전 카드 + 위치 공유 | `SafetyCard` | P4→P5 |
| F3 | 갯벌 채집 도감 + 금지종 경보 | `CatchGuide` | P2(모델)→P4→P5 |

#### 그룹 G. 부가 기능
| ID | 기능명 | ERD 모델 | Phase |
|----|--------|----------|-------|
| G1 | 온천 효능 매칭 | `HotspringDetail` | P2(모델)→P4→P5 |
| G2 | 포용성 (반려견/무장애) | `WaterSpot.pet_allowed`, `.accessibility` | P2(모델)→P4→P5 |

---

## 공공데이터 API 통합 정리

| 기관 | Base URL | 주요 엔드포인트 | 연계 기능 | 갱신 주기 |
|------|----------|----------------|----------|----------|
| **한국관광공사 TourAPI** | `apis.data.go.kr/B551011/KorService2/` 계열 | Service2의 현재 공식 operation | 전 기능 (공간 베이스 레이어) | 필요 시 |
| **기상청** | `apis.data.go.kr/1360000/VilageFcstInfoService_2.0/` | `getVilageFcst`, `getUltraSrtNcst`, `getUltraSrtFcst` | A1, A2, C2, F1 | 1~3시간 |
| **기상청 중기예보** | `apis.data.go.kr/1360000/MidFcstInfoService/` | `getMidLandFcst`, `getMidTa` | A2 (7일 예보) | 6시간 |
| **기상청 생활지수** | `apis.data.go.kr/1360000/LivingWthrIdxService01/` | `getUVIdxV4` | A1 (자외선) | 일 1회 |
| **해양조사원 (KHOA)** | `apis.data.go.kr/1192136/` | 조석예보, 수온관측, 조류예보, 파고관측 | A1, A2, A4, A6, C2, C3 | 실시간~1시간 |
| **환경부 수질** | `apis.data.go.kr/B090026/WaterqualityService/` | `getInfo` (BOD, COD, pH, DO 등) | A1, A8, F1 | 월간/계절 |
| **해양수산부 바다누리** | 별도 | 해수욕장 수질등급, 이안류 위험도 | A1, A8 | 계절 |
| **관광공사 데이터랩** | 별도 | 방문량, 유동인구 | D2 | 일간 |

> ⚠️ 모두 data.go.kr 통합인증 (`serviceKey` 파라미터), 개발계정 1,000건/일
> ⚠️ 기상청은 **NX/NY 격자좌표** 사용 → 위경도↔격자 변환 함수 필수

---

## 우선순위 전략 (기획서 §10 추천 조합 반영)

```
B(Water Index + Forecast) = 두 개의 플래그십
    ↓
A5(라이브캠) = 데모용 비밀무기
    ↓
A(안전·ESG) = 사회가치 축
    ↓
C(페르소나 큐레이션) = 진입 UX
```

이를 반영하여 Phase 3~5의 구현 순서를 **A1→A2→A3~A5→F1→B1~B3→나머지** 순으로 배치합니다.

---

## Phase 1: Project Setup & Dockerization

### 목표
Git 저장소 초기화, Django·React 프로젝트 구조 수립, Docker 컨테이너화 (ARM64/RPi5 호환)

### 디렉토리 구조

```
Multtara/
├── .github/
│   └── workflows/              # Phase 6
├── backend/
│   ├── Dockerfile
│   ├── .dockerignore
│   ├── requirements.txt
│   ├── manage.py
│   ├── config/                  # Django 프로젝트 설정
│   │   ├── __init__.py
│   │   ├── settings/
│   │   │   ├── __init__.py
│   │   │   ├── base.py          # 공통 설정
│   │   │   ├── dev.py           # 개발 환경
│   │   │   └── prod.py          # 운영 환경
│   │   ├── urls.py
│   │   ├── wsgi.py
│   │   └── asgi.py
│   ├── apps/                    # Phase 2에서 모델 구현
│   │   ├── __init__.py
│   │   ├── users/               # User, UserActivity, Passport
│   │   ├── spots/               # WaterSpot, NearbyFacility, CatchGuide, HotspringDetail
│   │   ├── conditions/          # WaterCondition, ConditionScore, CrowdLevel
│   │   ├── forecasts/           # WaterForecast, GoldenMoment
│   │   ├── content/             # SoundProfile, SpotAnalytics, TripMemory
│   │   └── trips/               # Itinerary, SafetyCard
│   └── services/                # Phase 3에서 API 래퍼 + 산출 로직
│       ├── __init__.py
│       ├── tourapi.py           # 한국관광공사 TourAPI
│       ├── weather.py           # 기상청 (단기/중기/생활지수)
│       ├── marine.py            # 해양조사원 (수온/파고/조석/조류)
│       ├── water_quality.py     # 환경부 수질
│       ├── crowd.py             # 혼잡도 (데이터랩/유동인구)
│       ├── water_index.py       # ★ Water Index 산출식 (A1 핵심)
│       ├── water_forecast.py    # ★ Water Forecast 계산 (A2 핵심)
│       ├── golden_moment.py     # 만조×일몰 골든 캘린더 (C3)
│       ├── asmr_score.py        # ASMR 지수 계산 (C2)
│       ├── safety_radar.py      # 계곡 안전 레이더 (F1)
│       └── grid_converter.py    # 기상청 NX/NY ↔ 위경도 변환
├── frontend/
│   ├── Dockerfile
│   ├── .dockerignore
│   ├── nginx.conf
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   ├── public/
│   └── src/
│       ├── main.jsx
│       ├── App.jsx
│       ├── index.css            # 디자인 시스템 (Blue·Aqua·Modern·Minimal)
│       ├── components/
│       │   ├── common/          # Header, Footer, Card, Badge, ScoreGauge
│       │   ├── map/             # WaterTwinMap, TempOverlay, SpotMarker
│       │   ├── charts/          # ForecastChart, HeatmapCalendar, TideTimeline
│       │   ├── cards/           # WaterIndexCard, SpotCard, LivecamCard
│       │   └── modals/          # SafetyCard, CatchGuide, FilterModal
│       ├── pages/
│       │   ├── HomePage.jsx         # 메인: Water Index + Forecast + 추천 + 라이브캠
│       │   ├── MapPage.jsx          # Water Twin 전체 지도
│       │   ├── SpotDetailPage.jsx   # 스팟 상세 (컨디션/예보/동선/라이브캠/채집도감)
│       │   ├── ForecastPage.jsx     # 7일 예보 히트맵
│       │   ├── LivecamPage.jsx      # 라이브캠 물멍 허브
│       │   ├── ProfilePage.jsx      # Passport/도감/일정
│       │   └── OnboardingPage.jsx   # 페르소나 스와이프 (B1 진입 UX)
│       ├── hooks/
│       │   ├── useWaterIndex.js
│       │   ├── useForecast.js
│       │   └── useGeolocation.js
│       ├── services/            # Axios API 클라이언트
│       │   └── api.js
│       ├── store/               # Zustand 상태관리
│       │   ├── useSpotStore.js
│       │   ├── useUserStore.js
│       │   └── useFilterStore.js
│       └── utils/
│           ├── gridConverter.js # NX/NY ↔ 위경도 (프론트 측)
│           └── scoreColor.js    # Water Index 점수 → 색상 매핑
├── docker-compose.yml           # 운영용
├── docker-compose.dev.yml       # 개발용 (핫 리로드)
├── .env.example
├── .gitignore
└── README.md
```

### 생성 파일 목록

| # | 파일 | 설명 |
|---|------|------|
| 1 | `backend/Dockerfile` | Multi-stage Django 빌드 (ARM64) |
| 2 | `backend/.dockerignore` | 빌드 컨텍스트 제외 |
| 3 | `backend/requirements.txt` | Python 의존성 |
| 4 | `backend/manage.py` | Django 관리 스크립트 |
| 5 | `backend/config/__init__.py` | 패키지 init |
| 6 | `backend/config/settings/__init__.py` | 설정 init (환경별 자동 로드) |
| 7 | `backend/config/settings/base.py` | 공통 설정 (DRF, CORS, 앱 등록, DB) |
| 8 | `backend/config/settings/dev.py` | 개발 오버라이드 (DEBUG, SQLite 폴백) |
| 9 | `backend/config/settings/prod.py` | 운영 오버라이드 (보안, PostgreSQL) |
| 10 | `backend/config/urls.py` | URL 라우팅 (`/admin/`, `/api/v1/`, `/api/health/`) |
| 11 | `backend/config/wsgi.py` | WSGI 엔트리포인트 |
| 12 | `backend/config/asgi.py` | ASGI 엔트리포인트 |
| 13 | `backend/apps/__init__.py` | 앱 패키지 init |
| 14 | `frontend/Dockerfile` | Multi-stage React 빌드 (ARM64) |
| 15 | `frontend/.dockerignore` | 빌드 컨텍스트 제외 |
| 16 | `frontend/nginx.conf` | SPA 폴백 + API 프록시 + 정적 캐싱 |
| 17 | `frontend/*` (Vite 프로젝트) | React 앱 스캐폴딩 |
| 18 | `docker-compose.yml` | 운영 오케스트레이션 (PostgreSQL 튜닝) |
| 19 | `docker-compose.dev.yml` | 개발 오케스트레이션 (핫 리로드) |
| 20 | `.env.example` | 환경변수 템플릿 (DB/API키/Map) |
| 21 | `.gitignore` | Python + Node + Docker 무시 규칙 |
| 22 | `README.md` | 프로젝트 문서 + RPi5 배포 가이드 |

### 핵심 기술 결정

**Backend Dockerfile** (Multi-stage, ARM64):
```dockerfile
# Stage 1: Builder
FROM python:3.11-slim-bullseye AS builder
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential libpq-dev && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir --prefix=/install -r requirements.txt

# Stage 2: Runtime
FROM python:3.11-slim-bullseye
RUN apt-get update && apt-get install -y --no-install-recommends \
    libpq5 && rm -rf /var/lib/apt/lists/*
COPY --from=builder /install /usr/local
WORKDIR /app
COPY . .
EXPOSE 8000
CMD ["gunicorn", "config.wsgi:application", "--bind", "0.0.0.0:8000", "--workers", "3"]
```

**Frontend Dockerfile** (Multi-stage, ARM64):
```dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

**docker-compose.yml** (현재 계약):

초기 inline 예시는 약한 기본 비밀번호, wildcard host, 공개 backend port를 포함해
폐기했습니다. 개발은 `docker-compose.yml` + `docker-compose.dev.yml`, 운영은
release bundle의 `docker-compose.yml` + `docker-compose.deploy.yml`을 사용합니다.
운영 값은 `.env.example`의 fail-closed 형식을 따르고 Pi에서는
`scripts/pi-setup.sh`와 `scripts/pi-deploy.sh`만 사용합니다.

### 검증
```bash
# 개발 전용. 모든 published port는 기본적으로 loopback에만 bind된다.
docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build

# 운영 절차와 검증 명령은 docs/operations-runbook.md를 따른다.
scripts/test-ops-config.sh
```

---

## Phase 2: Database Modeling (Django ORM)

### 목표
기획서 §6 ERD의 **17개 엔티티**를 Django 모델로 구현, 마이그레이션 실행, Admin 등록

### Django 앱 ↔ ERD 매핑

#### `apps/users/` — 사용자·활동·인증

| 모델 | 기획서 | 필드 | 연계 기능 |
|------|--------|------|----------|
| **User** | §6 User | `persona_type` (onsen_healer\|surfer\|valley_explorer\|...), `mood_state`, `home_region`, `created_at` | B1, B5 |
| **UserActivity** | §6 UserActivity | `user_id(FK)`, `spot_id(FK)`, `action` (visit\|like\|review\|swipe_like\|swipe_skip), `rating`, `review_text`, `created_at` | B1 취향학습, A8 수질 교차검증 |
| **Passport** | §6 Passport | `user_id(FK)`, `spot_id(FK)`, `verified_at`, `badge_earned[]` (JSONField), `eco_action` | E1, E2, E3 |

> `User`는 Django `AbstractUser` 확장. `persona_type`은 choices로 정의.
> `UserActivity.review_text`는 A8 수질 교차검증에서 텍스트 분석에 사용.

---

#### `apps/spots/` — 물 여행지·부속 정보

| 모델 | 기획서 | 필드 | 연계 기능 |
|------|--------|------|----------|
| **WaterSpot** | §6 WaterSpot | `type` (sea\|pool\|hotspring\|valley\|lake\|waterpark\|waterfall\|tidal_flat\|riverside), `name`, `lat`, `lng`, `tourapi_id`, `tags[]` (JSONField), `livecam_url`, `pet_allowed`, `accessibility`, `region`, `address`, `image_url`, `description` | 전 기능의 베이스 |
| **NearbyFacility** | §6 NearbyFacility | `spot_id(FK)`, `type` (shower\|changing_room\|restaurant\|lodging\|parking\|local_food), `name`, `lat`, `lng`, `tag` (온천수빵/1급수백숙/양조장), `distance_min` | D1, D3 |
| **CatchGuide** | §6 CatchGuide | `spot_id(FK)`, `species[]` (JSONField), `banned_species[]` (JSONField), `best_time`, `season_restriction` | F3 |
| **HotspringDetail** | §6 HotspringDetail | `spot_id(FK)`, `minerals[]` (JSONField: sodium/sulfur/carbonate), `benefits[]` (JSONField: skin/neuralgia/fatigue) | G1 |

> `WaterSpot.type`의 9가지 유형은 기획서 §1 대상 여행지를 정확히 반영.
> `tags`는 기획서 B1의 태그 큐레이션 (#아이와함께_얕은물 등)에 사용.

---

#### `apps/conditions/` — 실시간 상태·점수·혼잡도

| 모델 | 기획서 | 필드 | 연계 기능 |
|------|--------|------|----------|
| **WaterCondition** | §6 WaterCondition | `spot_id(FK)`, `water_temp`, `air_temp`, `wind_speed`, `wave_height`, `water_quality_grade`, `rainfall_recent`, `water_level`, `tide_schedule` (JSONField: 간조/만조 시각), `rip_current_risk`, `uv_index`, `weather_alert`, `fetched_at` | A1 Water Index 입력값 전체 |
| **ConditionScore** | §6 ConditionScore | `spot_id(FK)`, `activity` (swim\|surf\|relax\|mudflat\|onsen\|rafting), `score` (0~100), `computed_at` | A1 산출 결과 저장 |
| **CrowdLevel** | §6 CrowdLevel | `spot_id(FK)`, `predicted_level`, `recommended_time`, `parking_availability`, `updated_at` | D2 혼잡도 |

> `WaterCondition`은 기획서 §7 API 호출 시나리오의 2~4단계 결과가 저장되는 모델.
> `ConditionScore`는 5단계(가중치 적용 → 계산 후 캐싱)의 결과.
> `ConditionScore.activity`의 6가지는 기획서 A1 산출식의 "활동별 가중치 차등"을 반영.

---

#### `apps/forecasts/` — 예보·골든모먼트

| 모델 | 기획서 | 필드 | 연계 기능 |
|------|--------|------|----------|
| **WaterForecast** | §6 WaterForecast | `spot_id(FK)`, `forecast_date` (D+1~D+7), `predicted_index`, `predicted_factors` (JSONField: 예측 근거), `computed_at` | A2 |
| **GoldenMoment** | §6 GoldenMoment | `spot_id(FK)`, `date`, `time`, `type` (high_tide_sunset\|sunrise\|...) | C3 만조×일몰 |

> `WaterForecast.predicted_factors`에는 예상 기온/수온/강수량/파고 등 근거 데이터를 JSON으로 저장.
> "주식 차트형 캘린더 히트맵"은 프론트에서 forecast_date별 predicted_index를 그래프로 렌더링.

---

#### `apps/content/` — 사운드·분석·추억

| 모델 | 기획서 | 필드 | 연계 기능 |
|------|--------|------|----------|
| **SoundProfile** | §6 SoundProfile | `spot_id(FK)`, `sound_type` (wave\|valley\|waterfall\|rain\|tidal), `asmr_score`, `audio_url` | C2 |
| **SpotAnalytics** | §6 SpotAnalytics | `spot_id(FK)`, `avg_water_temp_5y`, `quality_trend`, `crowd_trend`, `best_season` | C5 |
| **TripMemory** | §6 TripMemory | `user_id(FK)`, `spot_id(FK)`, `photo_url`, `taken_at`, `estimated_location` | C4 |

> `SoundProfile.asmr_score`는 services/asmr_score.py에서 파고·풍속 데이터로 계산.

---

#### `apps/trips/` — 일정·안전카드

| 모델 | 기획서 | 필드 | 연계 기능 |
|------|--------|------|----------|
| **Itinerary** | §6 Itinerary | `user_id(FK)`, `start_point`, `transport`, `is_day_trip`, `party_size`, `budget`, `schedule` (JSONField: [{time, spot_id, type}]) | B2, B3 |
| **SafetyCard** | §6 SafetyCard | `user_id(FK)`, `spot_id(FK)`, `condition_snapshot` (JSONField), `risk_factors` (JSONField), `nearest_safety_facility`, `shared_with[]` (JSONField) | F2 |

---

### 추가 작업
- 모든 모델 Django Admin 등록 (데이터 입력/확인용)
- `WaterSpot` 초기 시드 데이터 (TourAPI에서 주요 해수욕장/계곡/온천 50곳 수동 추출)
- `python manage.py makemigrations && python manage.py migrate`

### 검증
```bash
python manage.py makemigrations --check --dry-run  # 마이그레이션 정상 생성
python manage.py migrate                           # 테이블 생성
python manage.py shell -c "from apps.spots.models import WaterSpot; print(WaterSpot._meta.get_fields())"
python manage.py createsuperuser                   # Admin 접속 확인
```

---

## Phase 3: External API Integration & Core Logic

### 목표
4대 공공API 래퍼 구현, **Water Index(A1) 산출식** 및 **Water Forecast(A2) 계산 로직** 구현, 주기적 데이터 수집 파이프라인 구축

### 3-1. API 래퍼 모듈 (`services/`)

#### `services/tourapi.py` — 한국관광공사 TourAPI
```python
# 기획서 §7 시나리오 1단계: "지역 내 type=sea WaterSpot 목록 + 좌표 조회"
class TourAPIClient:
    # 역사 예시. 실제 구현은 TourAPI Service2 gateway 계약을 사용한다.
    BASE_URL = "https://apis.data.go.kr/B551011/KorService2/"
    
    def search_by_area(self, area_code, content_type_id)     # areaBasedList1
    def search_by_location(self, lat, lng, radius)           # locationBasedList1
    def search_by_keyword(self, keyword)                     # searchKeyword1  
    def get_detail(self, content_id)                         # detailCommon1
    def get_intro(self, content_id, content_type_id)         # detailIntro1
    def get_images(self, content_id)                         # detailImage1
    def get_nearby_restaurants(self, lat, lng, radius)       # D3 로컬 미식
    def get_nearby_lodging(self, lat, lng, radius)           # D1 동선
```

- `contentTypeId` 매핑: 12=관광지(해수욕장), 28=레포츠(수상), 15=축제(물축제), 25=여행코스
- 필수 파라미터: `serviceKey`, `MobileOS=ETC`, `MobileApp=PongDang`, `_type=json`

#### `services/weather.py` — 기상청 API
```python
# 기획서 §7 시나리오 2단계: "각 좌표의 기온/풍속/강수확률/자외선/특보 조회"
class WeatherAPIClient:
    # 단기예보 (VilageFcstInfoService_2.0)
    def get_current_weather(self, nx, ny)           # getUltraSrtNcst (실황)
    def get_short_forecast(self, nx, ny)            # getVilageFcst (단기예보)
    def get_ultra_short_forecast(self, nx, ny)      # getUltraSrtFcst
    
    # 중기예보 (MidFcstInfoService) — A2 Forecast용
    def get_mid_land_forecast(self, reg_id)          # getMidLandFcst (중기육상)
    def get_mid_temp_forecast(self, reg_id)           # getMidTa (중기기온)
    
    # 생활기상지수 — A1 UV 지수
    def get_uv_index(self, area_no)                  # getUVIdxV4
```

> ⚠️ `grid_converter.py`: 위경도(lat/lng) → 기상청 격자좌표(NX/NY) 변환 함수 구현 필수

#### `services/marine.py` — 국립해양조사원 + 바다누리
```python
# 기획서 §7 시나리오 3~4단계
class MarineAPIClient:
    # 해양조사원 (apis.data.go.kr/1192136/)
    def get_tide_prediction(self, obs_code, date)     # 조석예보 (A6 물때)
    def get_sea_temperature(self, obs_code)            # 수온관측 (A1, A4, A7)
    def get_wave_height(self, obs_code)                # 파고관측 (A1)
    def get_tidal_current(self, obs_code, date)        # 조류예보 (A1)
    def get_beach_info(self, obs_code)                 # 해수욕장 정보
    def get_sunrise_sunset(self, obs_code, date)       # 일출/일몰 (C3)
    
    # 바다누리 (해양수산부)
    def get_beach_water_quality(self, beach_code)      # 수질등급
    def get_rip_current_risk(self, beach_code)         # 이안류 위험도
```

#### `services/water_quality.py` — 환경부 수질
```python
# 기획서 §7 시나리오 (환경부): 하천/계곡 수질, 수위, 유량
class WaterQualityAPIClient:
    def get_water_quality(self, pt_name)    # BOD, COD, T-N, T-P, pH, DO, 대장균
    def get_water_level(self, station)      # 하천 수위 (F1 안전 레이더)
    def get_flow_rate(self, station)        # 유량 (F1, F3)
```

#### `services/crowd.py` — 혼잡도 데이터
```python
# 기획서 D2: 관광공사 데이터랩, 유동인구
class CrowdDataClient:
    def get_visitor_count(self, area_code, date)  # 방문량
    def estimate_crowd_level(self, spot_id)         # 혼잡도 예측
```

---

### 3-2. 핵심 산출 로직

#### `services/water_index.py` — ★ Water Index (A1 플래그십)

기획서 §4 A1 산출식을 정확히 구현:

```python
"""
Water Index = f(기온, 수온, 풍속, 파고, 강수확률/강수량, 미세먼지,
                수질등급, 이안류/특보, 자외선, 혼잡도)
→ 활동별 가중치를 달리해 복수 점수 산출 (물놀이용 / 서핑용 / 물멍용 / 갯벌용)
"""

# 활동별 가중치 테이블 (별도 설정값으로 분리해 튜닝 가능 — 기획서 §7 유의사항)
ACTIVITY_WEIGHTS = {
    "swim": {       # 물놀이용
        "air_temp": 0.15, "water_temp": 0.20, "wind_speed": 0.10,
        "wave_height": 0.15, "rainfall": 0.10, "water_quality": 0.10,
        "rip_current": 0.10, "uv_index": 0.05, "crowd": 0.05
    },
    "surf": {       # 서핑용 — 파고 가중치 ↑
        "wave_height": 0.30, "wind_speed": 0.20, ...
    },
    "relax": {      # 물멍용 — 혼잡도·경관 가중치 ↑
        "crowd": 0.25, "air_temp": 0.15, ...
    },
    "mudflat": {    # 갯벌용 — 물때·수온 가중치 ↑
        "tide_optimal": 0.30, "water_temp": 0.15, ...
    },
    "onsen": {      # 온천용
        "air_temp_inverse": 0.30, "crowd": 0.25, ...  # 추울수록 좋음
    },
    "rafting": {    # 래프팅용 — 수량·유속 가중치 ↑ (F3)
        "water_level": 0.30, "rainfall_recent": 0.25, ...
    },
}

# 장소 유형별 평가 요소 (기획서 §4 A1 표)
SPOT_TYPE_FACTORS = {
    "sea":        ["air_temp", "water_temp", "wind_speed", "wave_height", "rainfall", "dust"],
    "valley":     ["rainfall_recent", "air_temp", "water_quality", "water_temp_est", "crowd"],
    "hotspring":  ["air_temp_inverse", "season", "crowd", "user_satisfaction"],
    "tidal_flat": ["tide_optimal", "water_temp", "wind_speed"],
}

def calculate_water_index(condition: WaterCondition, activity: str) -> int:
    """0~100점 산출. 가중치는 ACTIVITY_WEIGHTS에서 참조."""
    ...
```

> 가중치는 별도 설정값(ACTIVITY_WEIGHTS dict)으로 분리하여 **운영 중 튜닝 가능** (기획서 §7 유의사항 반영)

#### `services/water_forecast.py` — ★ Water Forecast (A2 제2 플래그십)

```python
"""
기상청 중기예보·조석 예보의 미래 공개 값을 활용하여,
Water Index를 D+1 ~ D+7까지 예보 그래프로 산출.
같은 파이프라인에 forecast_date별로 반복 계산. (기획서 §7 유의사항)
"""
def compute_forecast(spot_id: int) -> list[WaterForecast]:
    # 1. 기상청 중기예보 (getMidLandFcst, getMidTa) → D+3~D+7 예상 기온/강수
    # 2. 기상청 단기예보 (getVilageFcst) → D+1~D+2 예상 기온/풍속/강수
    # 3. 해양조사원 조석예보 → D+1~D+7 예상 조석
    # 4. 각 날짜별 예상 WaterCondition 생성 → water_index 계산
    # 5. WaterForecast 테이블에 저장 (predicted_index, predicted_factors)
    ...
```

#### `services/golden_moment.py` — 만조×일몰 골든 캘린더 (C3)
```python
def find_golden_moments(spot_id: int, month: int) -> list[GoldenMoment]:
    """조석 예보의 만조 시각과 일몰 시각이 ±30분 이내로 겹치는 날 계산"""
    ...
```

#### `services/asmr_score.py` — ASMR 지수 (C2)
```python
def calculate_asmr_score(wave_height: float, wind_speed: float, spot_type: str) -> int:
    """파고·풍속 데이터로 파도 소리 강도 예측 (0~100)"""
    ...
```

#### `services/safety_radar.py` — 계곡 안전 레이더 (F1)
```python
def check_valley_safety(spot_id: int) -> dict:
    """실시간 하천 수위·유량 + 호우예보 결합 → 급류 위험 경보 판단"""
    # 위험도: safe | caution | warning | danger
    ...
```

#### `services/grid_converter.py` — NX/NY 격자 변환
```python
def latlon_to_grid(lat: float, lon: float) -> tuple[int, int]:
    """위경도 → 기상청 NX/NY 격자좌표 변환 (Lambert Conformal Conic)"""
    ...
```

---

### 3-3. 데이터 수집 파이프라인

#### Django Management Commands
```bash
python manage.py fetch_weather      # 기상청 데이터 수집 (1~3시간 주기)
python manage.py fetch_marine       # 해양조사원 수온/파고/조석 (1시간 주기)
python manage.py fetch_quality      # 환경부 수질 (일 1회)
python manage.py compute_index      # Water Index 전체 재계산
python manage.py compute_forecast   # Water Forecast D+1~D+7 계산
python manage.py compute_golden     # 골든모먼트 월간 계산
```

#### Celery + Redis 주기 스케줄링 (docker-compose에 추가)
```yaml
# docker-compose.yml에 추가
  redis:
    image: redis:alpine
    ports: ["6379:6379"]

  celery_worker:
    build: ./backend
    command: celery -A config worker -l info
    depends_on: [db, redis]
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      
  celery_beat:
    build: ./backend
    command: celery -A config beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    depends_on: [db, redis]
```

#### 캐싱 전략 (기획서 §7 유의사항: API 호출 제한 고려)
```python
# Django 캐시 설정 (Redis 백엔드)
CACHES = {
    "default": {
        "BACKEND": "django.core.cache.backends.redis.RedisCache",
        "LOCATION": "redis://redis:6379/1",
    }
}

# 갱신 주기 (1,000건/일 제한 고려)
CACHE_TTL = {
    "weather_current": 60 * 30,      # 30분 (기상 실황)
    "weather_forecast": 60 * 180,    # 3시간 (단기예보)
    "marine_temp": 60 * 60,          # 1시간 (수온)
    "marine_tide": 60 * 60 * 6,      # 6시간 (조석 — 예보값이므로)
    "water_quality": 60 * 60 * 24,   # 24시간 (수질 — 월간 갱신)
    "crowd_level": 60 * 60,          # 1시간
    "tour_spot_detail": 60 * 60 * 24, # 24시간 (관광지 기본정보)
}
```

### 검증
```bash
python manage.py fetch_weather --spot-id=1 --dry-run   # API 응답 확인
python manage.py compute_index --spot-id=1             # 산출 결과 확인
python manage.py shell -c "from apps.conditions.models import ConditionScore; print(ConditionScore.objects.filter(spot_id=1).values())"
```

---

## Phase 4: REST API Development (DRF)

### 목표
기획서 전체 기능에 대응하는 REST API 엔드포인트 구현. 필터링·검색·정렬 로직.

### API 엔드포인트 전체 설계

#### 컨디션·핵심 (그룹 A)
```
GET  /api/v1/spots/                           # WaterSpot 목록 (필터: type, region, tags, pet_allowed, accessibility)
GET  /api/v1/spots/{id}/                      # WaterSpot 상세
GET  /api/v1/spots/{id}/condition/            # 현재 WaterCondition (A1 입력값)
GET  /api/v1/spots/{id}/scores/               # ConditionScore 목록 (활동별 Water Index) [A1]
GET  /api/v1/spots/{id}/forecast/             # WaterForecast D+1~D+7 [A2]
GET  /api/v1/spots/{id}/tide/                 # 물때 스케줄 (tide_schedule) [A6]
GET  /api/v1/spots/ranking/                   # Water Index 기준 전국 랭킹 [A1]
GET  /api/v1/spots/nearby/?lat=&lng=&radius=  # 내 주변 스팟 (위치 기반)
GET  /api/v1/spots/first-swim/               # 올해 첫 입수 가능 해변 목록 [A7]
GET  /api/v1/spots/{id}/quality-trust/        # 수질 신뢰도 (공식 vs 리뷰 비교) [A8]
```

#### AI 추천 (그룹 B)
```
POST /api/v1/recommend/persona/               # 페르소나 기반 추천 [B1]
POST /api/v1/recommend/mood/                   # 감정 기반 추천 [B5]
POST /api/v1/recommend/concierge/              # 자연어 추천 [B3]
POST /api/v1/itinerary/generate/               # AI 일정 생성 [B2]
GET  /api/v1/itinerary/{id}/                   # 일정 상세 조회
```

#### 콘텐츠 (그룹 C)
```
GET  /api/v1/spots/{id}/sound/                 # 사운드 프로필 + ASMR 지수 [C2]
GET  /api/v1/spots/{id}/golden-moments/        # 골든 캘린더 [C3]
GET  /api/v1/spots/{id}/analytics/             # 장기 변화 데이터 [C5]
POST /api/v1/memories/                         # 추억 사진 업로드 [C4]
GET  /api/v1/memories/                         # 내 추억 목록
GET  /api/v1/spots/mulmung/?mood=static|dynamic # 물멍 지도 [C1]
```

#### 동선·편의 (그룹 D)
```
GET  /api/v1/spots/{id}/nearby-facilities/     # 주변 편의시설 (샤워장/탈의실/주차장) [D1]
GET  /api/v1/spots/{id}/food-pairing/          # 물의 맛 로컬 미식 [D3]
GET  /api/v1/spots/{id}/crowd/                 # 혼잡도 + 추천 방문 시간 [D2]
```

#### 게이미피케이션 (그룹 E)
```
POST /api/v1/passport/checkin/                 # 방문 인증 [E1]
GET  /api/v1/passport/badges/                  # 내 배지 목록 [E1, E2]
GET  /api/v1/passport/collection/              # 도감 진행률 [E2]
POST /api/v1/passport/eco-action/              # 플로깅 인증 [E3]
```

#### 안전 (그룹 F)
```
GET  /api/v1/spots/{id}/safety/                # 계곡 안전 레이더 (위험도) [F1]
POST /api/v1/safety-card/                      # 오프라인 안전 카드 생성 [F2]
GET  /api/v1/safety-card/{id}/                 # 안전 카드 조회
GET  /api/v1/spots/{id}/catch-guide/           # 갯벌 채집 도감 + 금지종 [F3]
```

#### 부가 (그룹 G)
```
GET  /api/v1/spots/{id}/hotspring-detail/      # 온천 효능 매칭 [G1]
GET  /api/v1/spots/?pet_allowed=true           # 반려견 동반 필터 [G2]
GET  /api/v1/spots/?accessibility=wheelchair   # 무장애 필터 [G2]
```

#### 사용자
```
POST /api/v1/auth/register/
POST /api/v1/auth/login/
GET  /api/v1/auth/profile/
PATCH /api/v1/auth/profile/                    # persona_type, mood_state 업데이트
POST /api/v1/activity/                         # 사용자 활동 기록 (visit/like/review/swipe)
```

### Serializer·ViewSet 구조
- `SpotListSerializer` (목록용, 경량) / `SpotDetailSerializer` (상세, 중첩)
- `ConditionScoreSerializer` — activity별 score 포함
- `ForecastSerializer` — 7일치 배열
- `django-filter` 활용: type, region, tags, score range, activity 필터

### 검증
```bash
python manage.py test apps/                                    # 전체 테스트
curl "http://localhost:8000/api/v1/spots/?type=sea&ordering=-score"  # 필터 동작
curl "http://localhost:8000/api/v1/spots/1/scores/"                  # Water Index 응답
curl "http://localhost:8000/api/v1/spots/1/forecast/"                # 7일 예보 응답
```

---

## Phase 5: Frontend Development

### 목표
기획서 §8 UI/UX 방향을 구현. 모바일 퍼스트, 반응형. 디자인 키워드: `Blue · Aqua · Modern · Minimal · Nature · Healing`

### 5-1. 메인 화면 (HomePage) — 기획서 §8 메인 구성

```
┌─────────────────────────────────┐
│  🌊 퐁당 PongDang               │  ← Header + 검색바
├─────────────────────────────────┤
│  📊 오늘의 Water Index          │  ← A1: 상위 스팟 가로 스크롤 카드
│  [92점 해운대] [85점 가평계곡]    │     ScoreGauge 컴포넌트 (0~100 원형 게이지)
├─────────────────────────────────┤
│  📅 이번 주 최적 시점            │  ← A2: 히트맵 캘린더 (7일)
│  ▓▓▒▒▓▓▓  토요일 추천!          │     ForecastChart 컴포넌트
├─────────────────────────────────┤
│  📍 내 주변 추천 장소            │  ← GPS 기반 nearby 스팟
│  [지도 미니맵 + 마커]            │
├─────────────────────────────────┤
│  🤖 AI 추천 여행지               │  ← B1: 페르소나 기반 추천
│  [카드 스와이프]                  │
├─────────────────────────────────┤
│  📹 라이브캠 / 인기 장소          │  ← A5: 라이브캠 그리드 (데모 비밀무기)
│  [실시간 영상 썸네일 × 4]         │
└─────────────────────────────────┘
```

### 5-2. Water Twin 지도 (MapPage) — A3, A4

- 지도 API (카카오/네이버) 통합
- **수온 색상 오버레이** (A4): 파란색(차가움) → 빨간색(따뜻함) 그라데이션
- **Water Index 마커**: 각 스팟에 점수 표시, 색상으로 적합도 직관 표현
- **유형별 레이어 토글**: 바다/계곡/온천/갯벌 필터
- **유형별 시각화 정보** (기획서 A3):
  - 바다: 수온, 파고, 풍속, 이안류 위험도, 수질 상태
  - 계곡: 수위, 수온, 유량, 수질, 급류 위험도
  - 온천: 혼잡도, 추천 방문 시간
  - 갯벌: 간조/만조, 체험 가능 시간

### 5-3. 스팟 상세 (SpotDetailPage)

```
┌─────────────────────────────────┐
│  [해운대해수욕장]      📹 라이브캠 │  ← A5 라이브캠 (있으면)
├─────────────────────────────────┤
│  Water Index                     │
│  🏊 물놀이 92  🏄 서핑 78         │  ← A1 활동별 점수 (ConditionScore)
│  😌 물멍 88    🦀 갯벌 --         │
├─────────────────────────────────┤
│  📊 7일 예보                     │  ← A2 ForecastChart
│  [주식 차트형 그래프]              │
├─────────────────────────────────┤
│  🌊 현재 컨디션                   │  ← WaterCondition 상세
│  수온 24℃ | 파고 0.5m | 수질 1등급│
│  물때: 간조 14:20 만조 20:35      │  ← A6 물때 타이머
├─────────────────────────────────┤
│  ⚠️ 안전 정보                    │  ← F1 계곡 안전 레이더 / 이안류
│  [오프라인 안전카드 다운로드]      │  ← F2
├─────────────────────────────────┤
│  🍽️ 물놀이 전후 동선              │  ← D1 Pre/Post 동선
│  샤워장(300m) | 탈의실(150m)      │
│  🍲 1급수 백숙(차량 5분)          │  ← D3 로컬 미식
├─────────────────────────────────┤
│  🦐 채집 도감 (갯벌 전용)         │  ← F3 CatchGuide
│  지금 잡을 수 있는: 조개, 게       │
│  ⛔ 금어기: 꽃게 (10~11월)       │
├─────────────────────────────────┤
│  ♨️ 온천 효능 (온천 전용)         │  ← G1 HotspringDetail
│  성분: 유황, 나트륨 → 피부, 피로회복│
├─────────────────────────────────┤
│  📈 장기 변화                    │  ← C5 SpotAnalytics
│  최근 5년 평균 수온 추이 차트       │
├─────────────────────────────────┤
│  📊 수질 신뢰도                   │  ← A8 공식 vs 리뷰 비교
│  공식: 1등급 | 리뷰: ★★★★☆        │
├─────────────────────────────────┤
│  🔊 ASMR 지수: 95점              │  ← C2 SoundProfile
│  [▶ 파도 소리 재생]               │
├─────────────────────────────────┤
│  📸 인생샷 골든타임                │  ← C3 GoldenMoment
│  이번 달 최적: 6/14 19:40         │
└─────────────────────────────────┘
```

### 5-4. 7일 예보 (ForecastPage) — A2

- **주식 차트형 캘린더 히트맵** (기획서 원문)
- X축: D+1~D+7, Y축: Water Index 점수
- 색상 히트맵: 빨강(높음=좋음) → 파랑(낮음=비추천)
- AI 추천 메시지: "이번 주 토요일 방문을 추천합니다"

### 5-5. 라이브캠 허브 (LivecamPage) — A5

- 라이브캠이 있는 스팟 그리드 뷰
- iframe 또는 HLS 스트림 재생
- 각 캠 옆에 현재 Water Index 표시
- "라이브 물멍" 모드 (전체화면 + ASMR 재생)

### 5-6. 온보딩/페르소나 (OnboardingPage) — B1

- **스와이프 UI**: '잔잔한 인피니티풀' vs '파도치는 서핑 해변' 이미지 카드
- **태그 큐레이션**: `#아이와함께_얕은물`, `#튜브타기좋은_유속`, `#인생샷_온천`, `#한적한_계곡`
- 결과 → `User.persona_type` 설정 → 맞춤 추천 활성화

### 5-7. 프로필 (ProfilePage) — E1, E2, E3, B2

- **Water Passport**: 방문 인증 지도 + 배지 컬렉션
- **도감 진행률**: 폭포 100선 / 온천 50선 / 계곡 100선 / 해수욕장 200선
- **에코 배지**: 플로깅 인증 내역
- **내 일정**: AI 생성 일정 목록

### 5-8. 추가 페이지

| 페이지 | 기능 | 연계 |
|--------|------|------|
| 물멍 지도 | 정적↔동적 무드 축 필터, 야경/일몰/드라이브 필터 | C1 |
| AI 컨시어지 | 자연어 입력 → 스팟 랭킹 + 추천 이유 | B3 |
| 추억 재현 | 사진 업로드 → 과거 vs 현재 비교 | C4 |

### 컴포넌트 라이브러리

| 컴포넌트 | 용도 | 사용 페이지 |
|----------|------|-----------|
| `ScoreGauge` | 0~100 원형 게이지 (Water Index) | Home, Detail, Map |
| `ForecastChart` | 7일 예보 차트/히트맵 | Home, Detail, Forecast |
| `TideTimeline` | 물때 타임라인 (간조↔만조) | Detail |
| `WaterTwinMap` | 지도 + 수온 오버레이 + 마커 | Map, Home(미니) |
| `LivecamCard` | 라이브캠 썸네일 + 현재 지수 | Home, Livecam, Detail |
| `SpotCard` | 스팟 카드 (사진 + 점수 + 유형) | Home, 추천, 검색 |
| `SwipeCard` | 좌우 스와이프 카드 (온보딩) | Onboarding |
| `SafetyBanner` | 안전 경보 배너 | Detail |
| `BadgeGrid` | 배지 그리드 | Profile |
| `CatchGuidePanel` | 채집 도감 패널 | Detail (갯벌) |

### 검증
- 각 페이지 모바일(375px) / 태블릿(768px) / 데스크톱(1200px) 반응형 확인
- Lighthouse 성능 점수 측정
- Water Twin 지도에서 수온 오버레이 + 마커 클릭 → 상세 페이지 이동 확인
- 라이브캠 스트림 재생 확인

---

## Phase 6: CI/CD Pipeline Setup

### 목표
GitHub Actions 워크플로우 구성, Docker 이미지 빌드·푸시, RPi 5 배포 자동화

### 현재 CI/CD와 Raspberry Pi 도구

- `.github/workflows/ci.yml`: backend/frontend 테스트, dependency audit,
  Compose 계약 검사, shell lint, `linux/arm64` image build
- `.github/workflows/release-images.yml`: tag-bound GHCR digest image,
  provenance/SBOM, checksum이 포함된 release bundle 생성
- `scripts/pi-setup.sh`: 검증된 대상 디렉터리와 실행 사용자 초기화
- `scripts/pi-deploy.sh`: digest-only pull, readiness, 원자적 release 전환,
  실패 시 이전 image rollback
- `scripts/postgres-*.sh`: 검증된 custom archive backup, dry-run retention,
  확인문이 필요한 restore

임의의 원격 `curl | sh`, mutable `latest` tag, Pi 현장 build, 평문 token pipe,
무검증 `docker compose up`은 현재 배포 계약이 아닙니다. 정확한 명령과 필수
환경값은 `docs/operations-runbook.md`를 따릅니다.

### 검증
- GitHub Actions 워크플로우 전체 green 확인
- GHCR image가 `sha256` digest, provenance, SBOM을 포함하는지 확인
- release manifest checksum과 Pi의 deploy/readiness/rollback을 확인
- 별도 Compose project와 volume에서 PostgreSQL restore drill 수행

---

## 전체 의존관계 다이어그램

```
Phase 1 (Setup)
    ↓
Phase 2 (DB Modeling) ─── 17개 ERD 엔티티 전체
    ↓
Phase 3 (API + Logic) ─── 4대 API 래퍼 + Water Index/Forecast 산출 + Celery
    ↓
Phase 4 (REST API) ───── DRF ViewSet/Serializer + 필터링/검색
    ↓
Phase 5 (Frontend) ───── 7개 페이지 + 10개 컴포넌트 + 지도 통합
    ↓
Phase 6 (CI/CD) ──────── GitHub Actions + RPi 5 배포
```

---

## 결정된 항목과 운영 입력

- 지도는 browser에 공개 가능한 Kakao JavaScript key만 사용하며 허용 도메인을
  제한한다. 서버 API key와 재사용하지 않는다.
- 현재 주기 작업은 별도 non-root collector management command가 bounded worker로
  실행한다. Redis/Celery를 현재 계약으로 가정하지 않는다.
- 인증은 Django session + CSRF 계약이며 JWT나 소셜 로그인을 현재 구현으로
  가장하지 않는다.
- provider credential이 없으면 수집 작업을 건너뛰고 안전성은 `UNKNOWN`을
  유지한다. 정적 관광 콘텐츠는 사용할 수 있지만 demo를 live로 표시하지 않는다.

## RPi 5 배포 주의사항

> [!CAUTION]
> 커널 설정을 문서에서 임의로 변경하지 않는다. 배포 bundle의 architecture,
> Compose version, digest image 검증을 통과한 뒤 실제 Pi에서 image 기동과 복구
> drill을 수행한다.

> [!TIP]
> **Pi에서 직접 빌드 금지:** release workflow가 만든 digest image만
> `scripts/pi-deploy.sh`로 배포한다.
