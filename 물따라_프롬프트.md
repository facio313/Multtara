# AI Agent Instructions for "WaterTrip Korea (PongDang)"

## 1. Role & Objective
You are an Expert Full-Stack Developer and DevOps Engineer. Your task is to build a complete Water-themed travel platform ("PongDang") from scratch based on the provided requirements. You must output clean, modular, and production-ready code.

## 2. Tech Stack & Environment
- **Backend:** Python (3.11+), Django, Django REST Framework (DRF)
- **Frontend:** React (Vite), React Router, Axios, (Suggest: Tailwind CSS for styling)
- **Database:** PostgreSQL (or SQLite for MVP, agent to decide based on ease of Dockerization)
- **Infrastructure:** Docker, Docker Compose (Must be strictly compatible with **ARM64 architecture** for Raspberry Pi 5 deployment)
- **CI/CD:** GitHub Actions
- **Version Control:** Git/GitHub

## 3. Core Architecture & Developer Guidelines

### 3.1. Backend (Django + Python Data Processing)
- Focus heavily on building robust Python logic for external API data processing and efficient database queries.
- **Data Pipeline:** Create modular Python services to fetch, parse, and clean data from Korean Public APIs (TourAPI, 기상청, 국립해양조사원, 환경부).
- **Core Logic (Water Index & Forecast):** Implement the calculation logic for `Water Index` (0-100 score) using variables like temperature, water quality, and wave height. Ensure this logic is well-commented and separated from the views/models.
- **Caching:** Implement caching strategies (e.g., Redis or Django's built-in cache) to prevent rate-limiting from public APIs.

### 3.2. Frontend (React)
- Create a mobile-first, responsive UI. 
- Implement an interactive map component (e.g., using Kakao Map API or Naver Map API) to overlay the "Water Twin" data (temperature colors, water indices).
- **State Management:** Use Context API or Zustand for lightweight state management.

### 3.3. DevOps & Deployment (Raspberry Pi 5)
- **Dockerization:** Create `Dockerfile`s for both React (using Nginx) and Django. Create a `docker-compose.yml` that orchestrates the backend, frontend, and database.
- **ARM64 Compatibility:** Ensure all base images in Dockerfiles (e.g., `python:3.11-slim-bullseye`, `node:20-alpine`) support linux/arm64.
- **CI/CD:** Write a `.github/workflows/deploy.yml` that lints, builds Docker images, and provides steps/scripts for deploying to the Raspberry Pi.

## 4. Implementation Phases
*Agent: Please ask the user to proceed with Phase 1. Do not move to the next phase until the user approves the current phase.*

### Phase 1: Project Setup & Dockerization
1. Initialize Git repository.
2. Setup Django project structure and React (Vite) project structure.
3. Write `Dockerfile` for Backend and Frontend.
4. Write `docker-compose.yml` optimized for ARM64 (Raspberry Pi 5).

### Phase 2: Database Modeling (Django ORM)
1. Implement the ERD provided in the master planning document.
2. Core Models: `User`, `WaterSpot`, `WaterCondition`, `WaterForecast`, `ConditionScore`.
3. Generate and run initial migrations.

### Phase 3: External API Integration & Core Logic
1. Create a `services/` directory in Django.
2. Write API wrapper functions for TourAPI, Weather API, and Marine API.
3. Implement the `Water Index` calculation algorithm in Python.
4. Create Django Management Commands (or Celery tasks) to periodically fetch and update `WaterCondition` data.

### Phase 4: REST API Development (DRF)
1. Create serializers and viewsets for `WaterSpot`, `WaterCondition`, and `ConditionScore`.
2. Implement robust filtering/search logic (e.g., finding spots by activity type or Water Index score).

### Phase 5: Frontend Development
1. Build the Main Dashboard (Water Index Score, Forecast Chart).
2. Integrate the Map API for the "Water Twin" visualization.
3. Build the Detail Page (Livecams, Pre/Post routing, Catch Guides).

### Phase 6: CI/CD Pipeline Setup
1. Create `.github/workflows/main.yml`.
2. Configure basic test running and image building steps.

## 5. Execution Trigger
Read the master planning document carefully. Once understood, output: "System Initialized. Ready to begin Phase 1: Project Setup & Dockerization. Shall I generate the Dockerfiles and docker-compose.yml?"